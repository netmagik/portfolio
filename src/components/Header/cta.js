import React from "react";

const cta = () => {
    return (
        <div className="cta">
            <div className="btn">Download Resume</div>
            <div className="btn-primary">Contact</div>
        </div>

    );
}

export default cta;